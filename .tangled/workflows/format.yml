when:
  - event: ["push", "pull_request"]
    branch: main

engine: nixery

dependencies:
  nixpkgs:
    - go
    - diffutils

steps:
  - name: Check formatting
    command: |
      diff <(gofmt -d .) <(echo -n)
