when:
  - event: ["push", "pull_request"]
    branch: main

engine: nixery

dependencies:
  nixpkgs:
    - go
    - gcc

steps:
  - name: Lint code
    command: |
      go vet -v ./...
  - name: Run tests
    environment:
      RAPID_CHECKS: 10_000
    command: |
      go test -v -race ./...
